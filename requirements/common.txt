# Core Django
Django~=5.1.6
djangorestframework~=3.15.2
asgiref~=3.8.1
channels~=4.2.0
channels-redis~=4.2.0
python-socketio~=5.11.0
uvicorn[standard]~=0.34.0

# Authentication
dj-rest-auth~=7.0.1
PyJWT<2.10.0
django-allauth~=65.4.1

# Database (Optional, if using PostgreSQL)
psycopg2-binary~=2.9.10

# Environment Variables (Optional)
django-environ~=0.12.0

# CORS Headers (Optional, for API development)
django-cors-headers~=3.13.0

# error monitoring
sentry-sdk~= 2.26.1

# DRF
djangorestframework-camel-case~=1.4.2
drf-spectacular~=0.28.0
djangorestframework_simplejwt~=5.5.0

requests~=2.32.3

# PDF
PyPDF2~=3.0.1
weasyprint~=62.3
pydyf<0.12.0
markdown~=3.7

# Pinecone
pinecone~=6.0.1

# OpenAI (2.8+ required for memu-py compatibility)
openai>=2.8.0
tiktoken~=0.8.0

# Claude/Anthropic (0.75.0+ includes structured outputs beta)
anthropic~=0.75.0

# Google Gemini (new unified SDK)
google-genai~=1.53.0

# LLaMA (via Ollama)
# ollama~=0.4.2 - upgrading it below to avoid conflict with httpx
ollama>=0.6.1

rq==2.2.0
django-rq==3.0.0
rq-scheduler==0.14.0
redis==5.2.1

pycryptodome==3.22.0


# weaviate
weaviate-client==4.14.1

#email
resend~=2.7.0

# Text splitting (updated to support langchain-core 1.x required by memu-py)
langchain-text-splitters>=0.4.0

# Spreadsheet processing
openpyxl~=3.1.5
# Optional legacy .xls support (used only if available)
xlrd~=2.0.1

# Video processing (for frame extraction and audio transcription)
opencv-python~=4.10.0
Pillow~=11.1.0
ffmpeg-python~=0.2.0

# Cross-conversation memory layer (requires Python 3.13+)
# Pinned to 1.2.x — v1.3.0 has a SQLAlchemy bug (duplicate 'url' column on resources table)
memu-py~=1.2.0
# memu-py with PostgreSQL uses postgresql+psycopg:// (requires psycopg3, not psycopg2)
psycopg[binary]~=3.2.0

# Energy/environmental impact tracking for LLM calls
ecologits>=0.10.0

# SyftBox (distributed file storage)
syft-core
syft-perm
